[PRD]
# Windows 聊天消息查询程序需求设计说明书

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | Windows 聊天消息查询程序需求设计说明书 |
| 文档版本 | V1.1 |
| 目标平台 | Windows 10/11 64 位 |
| 开发语言 | Python 3.11 或项目最终锁定的兼容版本 |
| Web 框架 | Flask |
| 打包工具 | PyInstaller |
| 最终交付物 | 可独立运行的 Windows EXE、`package.bat`、源代码、测试代码及使用说明 |

## 2. 背景与概述

用户本地已安装聊天软件及其命令行工具。该命令行工具能够向用户或群组发送文本、图片和文件，也能查询私聊或群聊的历史消息。

本项目需要开发一个 Python 程序，对聊天 CLI 进行稳定、可复用的封装，并周期性增量查询用户配置的群组消息。程序启动后同时提供仅本机可访问的配置管理页面，并自动使用默认浏览器打开该页面。配置修改后须立即生效，无需重启程序。

程序最终使用 PyInstaller 打包为 Windows EXE，通过 `package.bat` 完成标准化构建。当前版本只为后续“逐条判断消息并调用知识库 API”预留扩展点，不实际调用知识库。

## 3. 目标

- 将发送私聊消息、发送群聊消息、查询历史消息封装为独立、可测试的原子能力。
- 支持按用户配置的周期，对多个指定群组依次执行增量历史消息查询。
- 除首次游标初始化外，定时查询必须使用上次保存的消息 ID，并固定向更新方向查询，以降低查询时延和数据量。
- 每次查询完成后输出结构化日志，明确记录群组 ID 和查询出的消息数量。
- 提供简单、安全、仅本机可访问的配置管理页面，并使配置变更实时生效。
- 将配置和查询游标持久化到 EXE 所在目录，重启后能够恢复。
- 生成可在 Windows 10/11 64 位环境直接运行的 EXE，并在打包期间输出版本号。
- 形成可扩展的模块边界，便于后续增加配置项、消息处理规则和知识库 API 调用。

## 4. 术语

| 术语 | 含义 |
| --- | --- |
| CLI 前缀 | 聊天命令行程序的可执行文件名或绝对路径，例如 `chat-cli` 或 `C:\\Tools\\chat-cli.exe` |
| 原子能力 | 不依赖 Web 页面和定时器、可被其他模块直接调用的单一聊天操作 |
| 查询游标 | 某群组已处理到的最大消息 ID，用于下次增量查询 |
| 初始化查询 | 某群组还没有持久化查询游标时，为获得最近消息及初始游标而执行的一次查询 |
| 增量查询 | 携带 `--message-id` 和 `--query-direction 1`、只查询游标之后消息的查询 |
| 实时生效 | 保存配置后，正在运行的程序无需重启即可按新配置工作 |

## 5. 用户与使用场景

目标用户为在 Windows 电脑上已安装并登录聊天软件、且能够正常使用聊天 CLI 的人员。

典型流程：

1. 用户双击 EXE。
2. 程序读取 EXE 所在目录中的本地配置；文件不存在时创建默认配置。
3. 程序启动本地 Web 服务与定时调度器，并自动打开默认浏览器进入配置页面。
4. 用户填写 CLI 前缀、目标群组 ID（每行一个）、查询开关和执行周期并保存。
5. 新配置立即通知调度器；开关打开时，调度器按周期查询消息。
6. 首次查询建立该群组游标；后续查询从游标位置向更新方向增量获取消息。
7. 每次查询输出日志并持久化成功处理后的最新游标。
8. 后续版本可在消息处理扩展点中逐条判断消息并调用知识库 API。

## 6. 范围

### 6.1 本期范围

- Windows EXE 主程序。
- CLI 命令执行与结果解析。
- 私聊消息发送原子能力。
- 群聊消息发送原子能力。
- 私聊/群聊历史消息查询原子能力。
- 多个指定群组的串行周期性增量查询。
- 查询游标持久化。
- 配置读取、校验、保存和热更新。
- Flask 配置管理页面。
- 文件和控制台日志。
- 知识库消息处理接口占位实现。
- PyInstaller 配置及 `package.bat`。
- 自动化测试和打包验证。

### 6.2 非目标

- 本期不调用任何知识库 API。
- 本期不实现消息内容判断、自动回复或消息转发业务。
- 本期不在配置页面提供手工发送消息或浏览历史消息的功能。
- 本期不并发查询多个群组；多个群组由同一调度线程依次查询。
- 本期不提供 Windows 服务、开机自启动、安装包、自动升级或系统托盘功能。
- 本期不提供远程访问、用户账号体系或多用户权限管理。
- 本期不替代聊天软件登录，不保存聊天账号密码。
- 本期不负责安装或升级聊天 CLI。

## 7. 已确认的产品决策

| 决策项 | 结论 |
| --- | --- |
| 配置页面方案 | Flask + 原生 HTML/CSS/JavaScript |
| 启动行为 | 启动本地 Web 服务后自动打开系统默认浏览器 |
| 数据目录 | EXE 所在目录 |
| 首次查询 | 获取最近 N 条消息，保存最大消息 ID，作为后续增量游标 |
| 后续查询 | 必须携带 `--message-id <cursor>` 和 `--query-direction 1` |
| 质量门禁 | `pytest`、`ruff check`、PyInstaller 打包成功、浏览器人工验收 |

## 8. 功能需求

### FR-001 CLI 前缀管理

1. 系统必须支持用户配置聊天 CLI 的可执行文件名或绝对路径。
2. CLI 前缀仅代表一个可执行文件，不允许把管道符、重定向符或额外命令参数作为前缀的一部分。
3. 系统执行命令时必须使用参数数组并禁止 `shell=True`，避免命令注入及路径空格问题。
4. 保存配置时应验证 CLI 前缀非空；运行时若找不到可执行文件，应记录明确错误，不得导致主程序退出。

### FR-002 发送私聊消息原子能力

提供内部 Python 接口，语义示例：

```python
send_to_user(
    receiver: str,
    text: str | None = None,
    image: Path | None = None,
    file: Path | None = None,
) -> SendResult
```

要求：

- 生成命令：`<cli-prefix> im send-to-user --receiver <receiver> ...`。
- `receiver` 必填。
- `text`、`image`、`file` 至少提供一项；支持同时提供 CLI 已允许的多项。
- 图片或文件参数必须传入规范化路径；文件不存在时在执行 CLI 前返回校验错误。
- 能解析纯 JSON输出，也能解析 `status_code: ...` 与 `resp_body: {...}` 组合输出。
- `resultCode == "0"` 视为业务成功，并返回消息 ID 列表和服务端发送时间。
- 非零退出码、超时、不可解析输出、非零 `resultCode` 必须转换为统一异常或失败结果。

### FR-003 发送群聊消息原子能力

提供内部 Python 接口，语义示例：

```python
send_to_group(
    group_id: str,
    text: str | None = None,
    image: Path | None = None,
    file: Path | None = None,
) -> SendResult
```

除目标参数使用 `--group-id` 外，其校验、执行、解析和错误处理规则与 FR-002 一致。群组 ID 在程序内部和配置文件中统一使用字符串，避免前端 JavaScript 数值精度损失。

### FR-004 历史消息查询原子能力

提供内部 Python 接口，语义示例：

```python
query_history_messages(
    *,
    group_id: str | None = None,
    user_account: str | None = None,
    query_count: int = 20,
    message_id: str | None = None,
    query_direction: int | None = None,
) -> HistoryQueryResult
```

要求：

- `group_id` 与 `user_account` 必须且只能提供一个。
- `query_count` 必须为正整数，并受合理上限约束；本期建议上限 100。
- `query_direction` 只允许 `0` 或 `1`。
- 若提供 `message_id`，必须同时提供 `query_direction`；反之亦然。
- 返回统一模型，至少包含 `messages`、`max_message_id`、`min_message_id`、`total_count` 和原始响应中的结果信息。
- 消息模型至少包含：`msg_id`、`content`、`content_type`、`sender`、`receiver`、`group_id`、`server_send_time`、`at` 和 `at_account_list`。
- 使用 `resultCode == "0"` 判断业务执行成功，并使用 `msgTotalCount` 判断本次是否存在消息。`msgTotalCount == 0` 时允许响应省略 `chatInfo` 并返回空消息列表；`msgTotalCount > 0` 时缺失有效 `chatInfo` 不得伪造成功结果。
- CLI 执行设置可配置或代码常量形式的超时，建议默认 30 秒。
- 正确处理 Windows 控制台编码；优先使用 UTF-8，解码失败时采用系统编码回退并记录告警。

### FR-005 配置项

本期配置模型如下：

| 键名 | 页面名称 | 类型 | 默认值 | 校验规则 |
| --- | --- | --- | --- | --- |
| `schema_version` | 不展示 | 整数 | `2` | 大于等于 1 |
| `cli_prefix` | 消息命令前缀 | 字符串 | `chat-cli` | 非空，表示可执行文件名或绝对路径 |
| `scheduled_query_enabled` | 定时查询指定群组开关 | 布尔值 | `false` | 仅允许 true/false |
| `target_group_ids` | 指定群组 ID | 字符串数组 | `[]` | 每项为数字字符串；开关打开时至少一项；页面每行一个 |
| `log_group_message_content` | 打印群组消息日志 | 布尔值 | `false` | 仅允许 true/false，保存后实时生效 |
| `query_interval_seconds` | 定时执行周期（秒） | 整数 | `60` | 建议范围 5～86400 |
| `initial_query_count` | 首次查询条数 | 整数 | `2` | 范围 1～100；可作为高级配置展示 |

说明：原始配置清单中未列出“指定群组 ID”，但定时查询能力要求用户自定义群组，因此本说明书将其补充为必需配置项。

### FR-006 配置持久化

1. 配置文件名为 `config.json`，位于 EXE 所在目录。
2. 开发环境运行源码时，数据目录默认为项目运行目录，并允许测试通过依赖注入指定临时目录。
3. 文件不存在时创建默认配置；文件内容损坏时不得覆盖原文件，应记录错误并使用安全默认配置启动，页面显示配置加载错误。
4. 保存时必须先完整校验全部字段。
5. 写入采用“同目录临时文件 + 原子替换”，避免进程中断产生半写文件。
6. JSON 文件使用 UTF-8 编码、可读缩进，并包含 `schema_version`，为后续迁移保留能力。
7. 未识别的扩展字段在合理情况下应保留，或由显式迁移逻辑处理，不得因保存当前页面字段而静默丢失未来配置。
8. EXE 所在目录不可写时，程序应显示并记录可操作的错误提示，不得假装保存成功。使用说明需提示不要把免安装程序放在普通用户无写权限的目录（例如受保护的 `Program Files` 目录）。

配置文件示例：

```json
{
  "schema_version": 2,
  "cli_prefix": "chat-cli",
  "scheduled_query_enabled": false,
  "target_group_ids": ["987432812330259203", "987432812330259204"],
  "log_group_message_content": false,
  "query_interval_seconds": 60,
  "initial_query_count": 2
}
```

### FR-007 配置管理页面

1. Web 服务仅监听 `127.0.0.1`，默认端口建议为 `8765`，不得监听所有网卡。
2. 程序启动且服务可用后，自动打开 `http://127.0.0.1:<port>/`。
3. 页面显示 FR-005 中的用户配置项、当前程序版本和服务运行状态。
4. 页面采用中文标签，说明周期单位为秒。
5. 保存按钮调用本地 JSON API；保存期间禁用重复提交。
6. 保存成功后显示明确成功反馈；校验或持久化失败时显示具体字段错误。
7. 页面刷新后展示已落盘配置，而不是浏览器内的临时状态。
8. 页面至少适配常见桌面浏览器宽度，并支持键盘操作和清晰的输入焦点。
9. 页面不依赖公网 CDN 或远程资源，所有静态资源随 EXE 打包。

建议 API：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/config` | 获取当前配置、版本和运行状态 |
| `PUT` | `/api/config` | 校验、落盘并发布新配置 |
| `GET` | `/api/health` | 返回服务健康状态和版本 |

### FR-008 配置实时生效

1. `PUT /api/config` 只有在“校验成功、持久化成功、内存配置替换成功”后才返回成功。
2. 配置管理器在替换配置后必须向调度器发送变更通知，不使用轮询配置文件的方式实现热更新。
3. 开关由关改为开：调度器立即重新计算计划；建议立即执行一次查询，然后按新周期继续。
4. 开关由开改为关：尚未开始的查询必须取消；已进入 CLI 子进程的查询允许完成，但完成后不得继续安排下一次。
5. 周期改变：下一次执行时间从配置保存成功的时刻按新周期重新计算。
6. 群组 ID 列表改变：新增群组使用自己的独立游标，移除群组后不再调度；不得在群组之间共用游标。
7. CLI 前缀改变：下一次 CLI 调用使用新值；正在执行的调用不强制中断。
8. 配置连续快速保存时，不得创建多个并行调度循环。
9. 消息日志开关改变：下一条待记录消息读取最新值，不需要等待下一轮查询或重启程序。

### FR-009 定时查询与并发控制

1. 调度器运行于独立后台线程，不阻塞 Flask 请求线程。
2. 同一程序实例同一时间最多执行一个定时查询。
3. 若一次查询耗时超过一个周期，不补跑所有错过的周期；本次完成后按当前配置重新计算下一次执行时间。
4. 调度器必须可被配置变更事件及时唤醒，以实现开关和周期的实时生效。
5. CLI 查询失败时记录错误并保留旧游标，下个周期重试。
6. 单次消息处理失败时不得把游标推进到未成功处理的消息之后。当前占位处理器默认对每条合法消息返回成功。
7. 程序关闭时应停止接受新调度任务，并在有限时间内等待正在执行的任务结束。

### FR-010 首次查询与游标初始化

1. 游标文件名为 `runtime_state.json`，位于 EXE 所在目录，与用户配置分离。
2. 游标按群组 ID 存储，以便未来支持多群组及用户切换群组后恢复进度。
3. 若目标群组无游标，执行一次初始化查询，获取最近 `initial_query_count` 条消息。
4. 初始化查询返回后，消息按 `serverSendTime`、`msgId` 的稳定升序交给消息处理占位接口。
5. 全部消息处理成功后，将响应中的 `maxMsgId`（或消息集合中的最大 `msgId`）保存为该群组游标。
6. 初始化查询也必须输出群组 ID 和消息条数日志。
7. 初始化查询失败、响应无效或消息处理失败时，不创建或推进游标。

首次查询的 CLI 是否支持在同时携带 `--message-id` 的情况下表达“最近 N 条”尚未由现有示例证明。因此本期暂定：初始化查询允许只使用 `--group-id` 和 `--query-count`；初始化完成后的每次定时增量查询均强制使用两个游标参数。编码前应通过目标环境联调确认；若 CLI 支持明确的“最新消息”哨兵 ID，则初始化查询也应改为携带 `--message-id` 与正确方向。

游标文件示例：

```json
{
  "schema_version": 1,
  "group_cursors": {
    "987432812330259203": {
      "message_id": "89307913870850767",
      "updated_at": "2026-08-07T12:00:00Z"
    }
  }
}
```

### FR-011 增量查询

1. 有游标时生成的命令必须包含：

   ```text
   <cli-prefix> im query-history-message --group-id <group-id> --query-count <count> --message-id <cursor> --query-direction 1
   ```

2. 只处理 `msgId` 大于当前游标的消息；CLI 重复返回游标消息时必须去重。
3. 消息处理顺序为从旧到新，稳定排序规则为 `serverSendTime` 升序、`msgId` 升序。
4. 一页返回达到 `query_count` 且可能仍有更多新消息时，调度器应在同一轮中继续分页查询，下一页使用上一页成功处理后的最大消息 ID。
5. 为防止异常 CLI 导致无限分页，单轮查询设置最大页数，建议默认 100 页；达到上限时记录错误并等待下个周期继续。
6. 每一页的消息全部成功经过处理扩展点后，才原子持久化该页最大消息 ID。这样进程中断时最多重复处理一页，不能丢消息。
7. 空结果时保留现有游标。

### FR-012 消息处理扩展点

提供独立处理器接口，语义示例：

```python
class MessageProcessor(Protocol):
    def process(self, message: ChatMessage) -> None: ...
```

本期默认实现 `NoOpMessageProcessor`：接收每条消息但不调用网络、不修改消息、不发送回复，并返回处理成功。调度器只依赖该接口，不直接包含未来知识库业务逻辑。

### FR-013 日志

1. 日志同时输出到控制台和 EXE 所在目录的 `logs/app.log`。
2. 日志至少包含时间、级别、模块/事件和正文。
3. 每次初始化查询或定时查询成功后必须由 `cliclient` 输出一条汇总日志，将群组 ID、CLI 返回的消息条数与耗时合并为一行，例如：

   ```text
   2026-08-08 15:07:32,121 - INFO - cliclient - [987432812330259203] count=2 elapsed_seconds=8.092
   ```

4. 查询失败日志包含群组 ID、错误类别、CLI 退出码（如有）及可安全展示的错误摘要。
5. 不在日志中输出完整文件内容、潜在敏感配置或无长度限制的 CLI 原始输出。
6. 建议使用按大小轮转日志，例如单文件 10 MB、保留 5 个历史文件。
7. 日志写入失败不得导致核心进程退出，但应尽可能在控制台报告。
8. `log_group_message_content` 关闭时不得输出消息正文；开启时由 `scheduler` 按 `[group_id] sender ➔ content` 逐条输出群组 ID、发送者和经过换行转义的消息正文，正文最多记录 4096 个字符。
9. 日志开关通过配置变更通知实时生效，正在处理的一页消息从下一条消息开始读取最新开关值。
10. 历史查询的消息数量和以秒为单位的 `elapsed_seconds` 必须在同一条成功日志中输出，不得再输出重复的查询完成日志；失败时记录操作名、耗时、错误类别和可用的退出码。
11. CLI 日志不得输出消息文本、接收者、附件路径等敏感命令参数；历史查询成功日志仅额外输出目标群组 ID。
12. 新增独立文件 `logs/messages.log`；当 `log_group_message_content` 开启时，每条成功处理的完整消息按 `日志记录时间|msgId|groupType|contentType|serverSendTime|groupId|sender|receiver|content` 写入一行。
13. `messages.log` 中的 `serverSendTime` 将 CLI 返回的 Unix 毫秒时间戳转换为带毫秒精度和 `Z` 后缀的 UTC ISO 8601 标准时间；无法识别的值保留原文。
14. `messages.log` 的消息正文不得截断；反斜杠、字段分隔符、回车和换行必须转义，以保证单条记录不跨行且字段可还原。
15. `messages.log` 写入失败时不得推进当前页游标，确保后续查询能够重试该消息。
16. 引用回复类型 `CARD_MSG` 的日志正文必须解析为 `cardContext.replyMsg.content + "↩" + cardContext.preMsg.content`；解析失败或结构不匹配时回退原始 `content`。
17. 新增独立轮转文件 `logs/operations.log`，记录程序启动、停止以及 Web 页面配置修改的成功、拒绝和失败操作；这些记录不得重复写入 `app.log`。
18. 启动记录至少包含程序版本和本地 Web 地址；配置修改成功记录发生变化的配置项名称，校验失败记录错误字段名称，持久化或未知异常记录错误类别。
19. `operations.log` 不得记录 CLI 路径、群组 ID 等配置值或完整请求体；单文件大小与保留数量沿用 `app.log` 的 10 MB、5 份轮转策略。

### FR-014 CLI 结果与错误模型

统一结果至少包含：

- `success`
- `result_code`
- `result_context`
- `status_code`（CLI 有输出时）
- 结构化业务数据
- 必要的诊断摘要

统一错误类型至少区分：配置/参数错误、CLI 不存在、CLI 超时、进程退出失败、输出解析失败、聊天服务业务失败和本地持久化失败。Web API 返回适当的 HTTP 状态码，后台任务捕获错误并记录，不允许异常终止调度线程。

### FR-015 版本管理与打包

1. 项目版本号使用单一来源，例如 `src/<package_name>/version.py` 中的 `__version__`。
2. Web 页面和 `/api/health` 显示同一版本号。
3. 程序支持 `app.exe --version` 打印版本号后退出。
4. 根目录提供 `package.bat`，至少完成：检查 Python、显示待打包版本、安装/校验构建依赖、清理本项目构建产物、执行 PyInstaller、检查目标 EXE 是否生成、打印成功信息和版本号。
5. PyInstaller 使用 `.spec` 文件显式包含 Flask 模板和静态资源。
6. 默认采用 `--onefile`、`--windowed` 或等效 spec 配置生成单 EXE；为了保留用户要求的运行日志，可在正式决定隐藏控制台前提供 `--console` 构建，或确保文件日志和页面状态足以诊断。V1 建议使用 `--console`。
7. `package.bat` 任一步骤失败时返回非零退出码，不得继续输出构建成功。
8. 构建输出统一放入 `dist\`，最终文件名必须包含单一版本源中的版本号，例如 `chat-message-agent-v0.2.0.exe`。
9. 构建过程不得把开发机上的 `config.json`、`runtime_state.json` 或日志打进 EXE。
10. Windows EXE 的 `FileVersion` 和 `ProductVersion` 文件属性必须与 `src/chat_message_agent/version.py` 及文件名中的版本一致。

## 9. 非功能需求

### NFR-001 安全

- Flask 只绑定回环地址 `127.0.0.1`。
- 不启用 Flask debug 模式和交互式调试器。
- CLI 调用不经过 shell。
- API 只接受 JSON，执行严格类型和范围校验。
- 页面设置合理的安全响应头；本期无远程访问场景，不引入不必要的认证系统。

### NFR-002 可靠性

- CLI 单次失败不能使 Web 服务或调度线程退出。
- 配置与游标采用原子写入。
- 游标只在消息成功处理之后推进。
- 重启后恢复配置和各群组游标。
- 后台线程的未捕获异常必须由顶层保护捕获并记录。

### NFR-003 性能

- 配置页面本地打开后，正常情况下 2 秒内可交互。
- 配置保存成功后，调度器在 1 秒内收到变更通知。
- Web 请求不得等待周期性 CLI 查询完成。
- 空闲时程序不应通过高频轮询持续占用明显 CPU。

### NFR-004 可维护性

- CLI、配置、状态、调度、消息处理、Web 和日志模块职责分离。
- 所有外部依赖固定版本或使用受控版本范围，并提供 `requirements.txt` 或等效依赖文件。
- 核心业务逻辑不依赖 Flask 请求上下文，便于单元测试和后续复用。
- 数据模型使用 `dataclass`、Pydantic 或同类清晰类型方案；项目内保持一致。

### NFR-005 可用性

- EXE 双击后无需命令行操作即可进入配置页面。
- 字段说明和错误提示使用中文。
- CLI 不存在、目录不可写、群组 ID 无效等常见问题给出可操作建议。

## 10. 建议架构

```text
Bootstrap / Application
├── Version
├── ConfigManager
│   ├── config.json
│   ├── validation
│   └── change notification
├── StateStore
│   └── runtime_state.json
├── ChatCliClient
│   ├── CommandRunner
│   ├── OutputParser
│   ├── send_to_user
│   ├── send_to_group
│   └── query_history_messages
├── QueryScheduler
│   ├── cursor initialization
│   ├── incremental pagination
│   └── MessageProcessor (NoOp in V1)
├── FlaskWeb
│   ├── HTML/templates
│   ├── static assets
│   └── JSON API
└── Logging
```

建议目录结构：

```text
project-root/
├── src/
│   └── chat_message_agent/
│       ├── __init__.py
│       ├── __main__.py
│       ├── app.py
│       ├── version.py
│       ├── models.py
│       ├── cli_client.py
│       ├── cli_parser.py
│       ├── config.py
│       ├── state.py
│       ├── scheduler.py
│       ├── processor.py
│       ├── logging_setup.py
│       └── web/
│           ├── routes.py
│           ├── templates/index.html
│           └── static/
├── tests/
│   ├── fixtures/
│   ├── test_cli_client.py
│   ├── test_cli_parser.py
│   ├── test_config.py
│   ├── test_state.py
│   ├── test_scheduler.py
│   └── test_web.py
├── requirements.txt
├── requirements-dev.txt
├── pyproject.toml
├── chat-message-agent.spec
├── package.bat
└── README.md
```

## 11. 数据与状态一致性设计

配置和游标必须使用不同文件：用户可编辑配置不应混入内部运行游标。每个存储组件内部使用锁保护同一进程内的读写，并通过临时文件原子替换目标文件。

单页消息的处理顺序为：

```text
执行 CLI 查询
→ 解析并校验响应
→ 过滤重复消息
→ 按时间和消息 ID 升序排序
→ 逐条调用 MessageProcessor
→ 全页处理成功
→ 原子保存该页最大消息 ID
→ 记录查询汇总日志
```

该设计保证“至少一次”处理语义：崩溃可能导致少量消息重复交给处理器，但不应因提前推进游标而永久漏消息。未来知识库调用必须设计幂等键，建议使用群组 ID + 消息 ID。

## 12. 用户故事与验收标准

### US-001：建立项目骨架和统一版本

**描述：** 作为开发者，我希望项目具有清晰结构和单一版本来源，以便持续开发和打包。

**验收标准：**

- [ ] 存在可安装或可直接运行的 `src` 包结构。
- [ ] 版本号只有一个权威定义位置。
- [ ] 源码模式支持 `python -m chat_message_agent --version`。
- [ ] 依赖文件区分运行依赖和开发/构建依赖。
- [ ] README 说明开发运行方式和 Windows 前置条件。

### US-002：实现安全的 CLI 命令执行与输出解析

**描述：** 作为开发者，我希望安全执行聊天 CLI 并获得统一结果，以便原子能力可靠复用。

**验收标准：**

- [ ] 命令通过参数数组执行且未使用 `shell=True`。
- [ ] 支持可执行文件绝对路径中包含空格。
- [ ] 可解析样例中的纯 JSON 和 `status_code`/`resp_body` 两种输出。
- [ ] 超时、CLI 不存在、非零退出码、无效 JSON 和非零业务码均有独立测试。
- [ ] 中文消息输出可正确解析，编码回退行为有测试。

### US-003：实现发送私聊消息原子能力

**描述：** 作为后续功能开发者，我希望调用 Python 接口发送私聊消息，以免重复拼接 CLI 命令。

**验收标准：**

- [ ] 正确生成接收者、文本、图片和文件参数。
- [ ] 文本/图片/文件至少一项的校验有效。
- [ ] 本地文件不存在时不启动 CLI 子进程。
- [ ] 成功结果返回消息 ID 和服务端发送时间。
- [ ] 使用模拟进程执行器完成单元测试，不依赖真实聊天软件。

### US-004：实现发送群聊消息原子能力

**描述：** 作为后续功能开发者，我希望调用 Python 接口发送群聊消息，以便复用统一错误处理。

**验收标准：**

- [ ] 正确生成 `im send-to-group` 和 `--group-id` 参数。
- [ ] 文本、图片、文件以及失败场景覆盖与私聊能力一致。
- [ ] 大整数形式的群组 ID 全程按字符串处理。

### US-005：实现历史消息查询原子能力

**描述：** 作为调度器，我希望按私聊或群聊目标查询历史消息，以便获取结构化消息列表。

**验收标准：**

- [ ] 私聊和群聊目标互斥校验有效。
- [ ] 可选参数能正确映射到 `--query-count`、`--message-id`、`--query-direction`。
- [ ] `message_id` 和 `query_direction` 必须成对出现。
- [ ] 样例响应被解析为统一消息和分页结果模型。
- [ ] 缺字段、空列表、业务失败和超时均有测试。

### US-006：实现配置持久化与校验

**描述：** 作为用户，我希望配置保存在 EXE 目录并在重启后恢复，以免重复设置。

**验收标准：**

- [ ] 首次启动创建带默认值和 schema 版本的 `config.json`。
- [ ] 有效配置保存后可重新加载且值一致。
- [ ] 无效配置不会覆盖已有有效文件。
- [ ] 写入使用临时文件和原子替换。
- [ ] 损坏文件、只读目录及未知扩展字段行为均有测试。

### US-007：实现群组游标状态存储

**描述：** 作为调度器，我希望独立持久化每个群组的最大消息 ID，以便重启和切换群组后继续增量查询。

**验收标准：**

- [ ] `runtime_state.json` 按字符串群组 ID 存储游标。
- [ ] 能独立读取、更新多个群组的游标。
- [ ] 游标使用字符串保存，避免精度损失。
- [ ] 状态写入为原子操作，并覆盖损坏状态和写入失败测试。

### US-008：实现定时增量查询调度器

**描述：** 作为用户，我希望程序按配置周期依次查询多个指定群组的新消息，以便后续自动处理。

**验收标准：**

- [ ] 开关关闭时不执行查询。
- [ ] 多个群组由同一调度线程依次查询，各自使用独立游标。
- [ ] 无游标时执行初始化查询并在处理成功后保存最大消息 ID。
- [ ] 有游标时命令必含 `--message-id` 和 `--query-direction 1`。
- [ ] 查询结果去重并按从旧到新处理。
- [ ] 满页时继续分页，空页停止；最大页数保护有效。
- [ ] CLI 或处理器失败时不错误推进游标。
- [ ] 一次查询跨过周期时不会并发执行或无限补跑。
- [ ] 使用可控时钟/事件和模拟 CLI 编写确定性测试，不使用真实等待 30 秒的测试。

### US-009：实现消息处理扩展点

**描述：** 作为后续开发者，我希望调度器通过稳定接口处理每条消息，以便未来接入知识库而不修改查询逻辑。

**验收标准：**

- [ ] 定义清晰的消息处理接口。
- [ ] V1 默认 No-Op 实现不调用任何外部 API。
- [ ] 每条消息均被调用一次，顺序可测试。
- [ ] 处理器抛出异常时当前页游标不推进。

### US-010：实现配置管理 API 与页面

**描述：** 作为用户，我希望在浏览器中查看和修改配置，以便无需编辑 JSON 文件。

**验收标准：**

- [ ] Flask 仅监听 `127.0.0.1` 且 debug 关闭。
- [ ] 页面展示 CLI 前缀、定时开关、群组 ID、周期和首次查询条数。
- [ ] `GET /api/config` 返回落盘配置和运行状态。
- [ ] `PUT /api/config` 对有效输入保存，对无效输入返回字段级错误。
- [ ] 页面不使用公网资源，打包后模板和静态文件可正常加载。
- [ ] 保存成功和失败均有明确可见反馈。
- [ ] 页面可通过键盘完成编辑和保存。

### US-011：实现配置热更新

**描述：** 作为用户，我希望保存配置后立即改变调度行为，以免重启 EXE。

**验收标准：**

- [ ] 开关打开后调度器立即开始工作，关闭后不再安排新查询。
- [ ] 修改周期后从保存时刻按新周期重新计时。
- [ ] 修改群组 ID 后使用新群组自己的游标。
- [ ] 修改 CLI 前缀后下一次调用使用新值。
- [ ] 快速连续保存不会产生多个调度线程或并发查询。
- [ ] API 只在配置落盘并发布成功后返回成功。

### US-012：实现启动、浏览器打开与优雅退出

**描述：** 作为用户，我希望双击 EXE 后自动看到配置页面，并在退出时安全停止后台任务。

**验收标准：**

- [ ] 服务确认可访问后只自动打开一次默认浏览器。
- [ ] 浏览器打开失败时程序继续运行并输出访问地址。
- [ ] 端口占用时输出明确错误，不静默退出。
- [ ] 退出流程停止新任务并有限等待运行中任务。
- [ ] `--version` 不启动 Web 服务或浏览器。

### US-013：实现可诊断日志

**描述：** 作为运维用户，我希望从控制台和文件看到查询结果与错误，以便定位问题。

**验收标准：**

- [ ] 日志输出到控制台和 `logs/app.log`。
- [ ] 每次查询汇总日志包含 `group_id` 和 `message_count`。
- [ ] 查询失败日志包含群组 ID 和错误分类。
- [ ] 文件日志会轮转，不无限增长。
- [ ] 日志目录不可写不会导致主程序异常退出。

### US-014：实现 Windows 打包流程

**描述：** 作为发布人员，我希望执行 `package.bat` 得到带版本信息的 EXE，以便重复构建和交付。

**验收标准：**

- [ ] `package.bat` 构建期间打印单一来源的版本号。
- [ ] PyInstaller spec 包含 Flask 模板和静态资源。
- [ ] 构建失败返回非零退出码，成功后 `dist\chat-message-agent.exe` 存在。
- [ ] EXE 执行 `--version` 输出与构建日志相同的版本号。
- [ ] 全新目录运行 EXE 后能创建默认配置、状态和日志目录。
- [ ] EXE 能打开配置页面并保存配置。

## 13. 质量门禁

以下检查适用于每个用户故事；与故事无关的检查可由实现者说明原因后跳过，但合并前必须全部通过：

```bat
python -m pytest -q
python -m ruff check .
```

发布候选版本还必须在 Windows 环境执行：

```bat
package.bat
dist\chat-message-agent.exe --version
```

UI 故事必须额外执行浏览器人工验证：

- 程序启动后自动打开配置页面。
- 默认值正确展示。
- 无效群组 ID、周期越界等错误可见。
- 保存有效配置后页面显示成功，刷新后值仍存在。
- 开关和周期修改无需重启即可改变调度行为。
- 页面在无公网连接时资源完整。

建议增加但不作为用户指定硬门禁的检查：

```bat
python -m ruff format --check .
```

## 14. 测试策略

### 14.1 单元测试

- 命令参数构造和禁止 shell 执行。
- 两种样例输出格式及异常输出解析。
- 配置默认值、校验、迁移预留和原子写入。
- 游标的按群组读取与原子推进。
- 初始化、增量、分页、去重、失败不推进游标。
- 配置变更通知和调度器重新计时。
- Web API 正常及错误响应。

### 14.2 集成测试

使用测试脚本或模拟可执行文件替代真实 `chat-cli`，验证完整子进程调用、Unicode、退出码、超时和响应解析。不要求 CI 环境安装真实聊天软件。

### 14.3 Windows 冒烟测试

- 在干净的 Windows 10/11 64 位环境运行 EXE。
- 验证无需本机 Python 环境即可启动。
- 验证 CLI 在 PATH 中和使用带空格绝对路径两种情况。
- 验证配置、状态、日志均写入 EXE 目录。
- 验证重启后配置和游标恢复。
- 验证真实 CLI 的首次查询参数语义。

## 15. 完成定义

满足以下全部条件才可视为 V1 完成：

- 所有本期用户故事及验收标准完成。
- 所有质量门禁通过。
- Windows 环境成功执行 `package.bat` 并生成 EXE。
- EXE 在未安装 Python 的测试机上可启动。
- 配置页面可打开、保存并实时影响调度。
- 真实或等价模拟 CLI 验证增量命令始终携带两个游标参数。
- 查询日志明确包含群组 ID 和消息数量。
- 知识库 API 未被调用，但消息处理扩展点存在并有测试。
- README 说明运行、配置、日志、打包及常见故障处理。

## 16. 成功指标

- 配置保存后 1 秒内通知调度器应用新配置。
- 正常运行时同一群组不会因游标提前推进而漏掉消息。
- 单次 CLI 调用失败不会导致程序退出，后续周期仍会重试。
- 每次查询均能在日志中通过群组 ID 和消息数量追踪。
- 在标准 Windows 10/11 64 位环境中，`package.bat` 可重复生成可运行 EXE。
- 所有自动化测试和静态检查通过。

## 17. 风险与处理措施

| 风险 | 影响 | 处理措施 |
| --- | --- | --- |
| 首次查询与强制 `message-id` 规则存在语义冲突 | 无法正确初始化游标 | 编码前用真实 CLI 确认哨兵 ID 或允许的一次性无游标初始化方案 |
| EXE 所在目录无写权限 | 配置、游标和日志无法持久化 | 启动时检测并明确提示；文档要求放在用户可写目录 |
| CLI 输出格式变化或混入额外日志 | JSON 解析失败 | 输出解析器隔离、保留受限诊断摘要并覆盖多格式测试 |
| 查询量超过单页 | 新消息延迟或遗漏 | 同轮增量分页、逐页提交游标、最大页数保护 |
| 定时任务与配置修改并发 | 使用旧配置或多任务并发 | 不可变配置快照、变更事件、单调度线程和锁 |
| PyInstaller 漏打包模板/静态资源 | 页面在 EXE 中不可用 | 使用 spec 显式收集资源并执行打包后 UI 冒烟测试 |
| `--onefile` 运行目录判断错误 | 配置写入临时解压目录 | 数据目录必须取 `sys.executable` 所在目录，不使用 `sys._MEIPASS` |

## 18. 待联调确认项

1. 真实 CLI 在无已有消息 ID 时，如何在同时携带 `--message-id` 的情况下表达“查询最近 N 条”。这是编码前唯一必须通过环境验证的协议项。
2. `--query-direction 1` 是否包含 `message-id` 对应消息；实现将按可能包含处理并通过 `msgId > cursor` 去重。
3. CLI 实际最大 `--query-count` 限制；确认后调整当前建议上限 100。
4. CLI 的默认字符编码和超时特征。
5. 真实 CLI 是否可能在标准输出 JSON 前后打印其他非 JSON 文本。

在上述事项未确认前，单元测试应基于本说明书提供的样例和明确假设实现，不阻塞其他模块开发。

## 19. 推荐实施顺序

1. US-001 项目骨架和版本。
2. US-002 CLI 执行器及解析器。
3. US-003、US-004、US-005 三项原子能力。
4. US-006、US-007 配置与游标存储。
5. US-009 消息处理扩展点。
6. US-008 调度器。
7. US-010、US-011 配置页面和热更新。
8. US-012、US-013 启停和日志。
9. US-014 Windows 打包及完整冒烟验收。

[/PRD]
